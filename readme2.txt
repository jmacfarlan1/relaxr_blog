
View Grade InfoGrade: N/A
Due: Tuesday, February 16, 2016 at 6:00 pm
Goal: Practice adding interactive elements to our project.

Requirements:

- Do this in the same repo as the original relaxr blog homework

- On clicking of the 'Sign up' button:

*Display an alert that says "We are not currently taking sign ups"
*Then change the text of the button to say "No Sign Ups!"
*- Select the appropriate DOM elements with CSS selectors upon a user's click using the $.click() handler

*- Add another paragraph to the posts.

-* If a user clicks "Read More":

*have the extra <p> tag slide down along with a "Read Less" link in the blog post using $.slideDown() and $.show()
*hide the "Read More" link using $.hide()
*- If a user clicks "Read Less":

*have the extra <p> slide up and hide the "Read Less" link using $.slideUp() and $.hide()
*show the "Read More" link using $.show()
*- Add an extra line of text in the sidebar under "About Relaxr" and wrap it an <span> that's nested in the <p> tag.

*- Using the same functions as above, if a user clicks the "Learn More" link in the sidebar:

*